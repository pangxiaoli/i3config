" 显示行号
set number

" 语法高亮
syntax on

" 显示当前模式
set showmode

" 显示当前命令
set showcmd

" 支持鼠标
set mouse=a

" 编码
set encoding=utf-8

" 启用 256 色
set t_Co=256

" tab 键长度
set tabstop=4

" 高亮光标所在行
set cursorline

" 行宽
set textwidth=90

" 自动折行
set wrap

" 不在单词内折行
set linebreak

" 显示状态栏
set laststatus=2

" 状态栏显示当前光标位置
set ruler

" 光标遇到括号自动匹配另一半
set showmatch

" 搜索时高亮显示匹配结果
set hlsearch

" 输入搜索模式时，每输入一个字符，就自动跳到第一个匹配的结果
set incsearch

" 搜索忽略大小写
set ignorecase

" tab 键命令补全
set wildmenu
set wildmode=longest:list,full

